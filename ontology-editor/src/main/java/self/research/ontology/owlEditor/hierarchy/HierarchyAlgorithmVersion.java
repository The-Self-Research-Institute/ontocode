package self.research.ontology.owlEditor.hierarchy;

/** Bump when top-level / children semantics change (invalidates stored snapshots). */
public final class HierarchyAlgorithmVersion {
    public static final String CURRENT = "v2";
    private HierarchyAlgorithmVersion() {}
}
